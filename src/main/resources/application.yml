info:
  app:
    name: AronimCloudConfigurationService
    description: Aronim Cloud Configuration Service
    version: 1.0-SNAPSHOT

spring:
  application:
    name: aronim-cloud-configuration
  cloud:
    config:
      server:
        git:
          uri: git@git.aronim.com:cloud/aronim-cloud-config-repo.git
          basedir: target/config
          cloneOnStart: true

server:
  port: 8888

